# Shared Schema Stub

Zod schemas for frontend live in `frontend/lib/schemas.js`.
Pydantic models for backend live in `ai-service/src/config/models.py`.
Both must mirror each other per `API_CONTRACT.md`.
